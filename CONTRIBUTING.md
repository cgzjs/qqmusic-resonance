# 参与贡献

感谢一起完善「同频」。为了让多人协作保持清晰，请遵循下面的约定。

## 开始开发

1. 从默认分支创建功能分支。
2. 使用 `feature/<name>`、`fix/<name>` 或 `docs/<name>` 命名。
3. 一次 Pull Request 只解决一个清晰问题。
4. 提交前运行 `npm run build`。

## 推荐的任务边界

- 视觉调整：只改对应组件和 `app/globals.css` 中的同名区域。
- 演示内容：只改 `lib/resonance/demo-data.ts`。
- 新业务字段：先更新 `lib/resonance/types.ts`，再更新数据和组件。
- 新流程：先在产品说明中补状态变化，再更新 `ResonanceExperience.tsx`。

## 代码风格

- 组件使用具名导出。
- Props 类型紧邻组件定义。
- 回调以 `on...` 命名，布尔值以 `is...` 或 `has...` 命名。
- 不在 JSX 中写大段匿名逻辑；复杂状态拆成变量或组件。
- 可交互元素使用原生按钮或项目现有 UI 基础组件。
- 用户可见文案使用中文，代码命名使用英文。

## Pull Request 检查

- [ ] 核心流程仍然可以完整走通
- [ ] 手机和桌面宽度下没有横向溢出
- [ ] 按钮有可理解的文本或 aria-label
- [ ] 没有把真实密钥、账号或个人位置数据提交进仓库
- [ ] `npm run build` 通过

## 产品边界

当前项目是可交互的作品集原型。请不要在没有讨论的情况下接入真实定位、蓝牙、QQ 音乐账号或付费曲库。
